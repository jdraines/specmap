# Phase 2+: local dev services
services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: specmap
      POSTGRES_USER: specmap
      POSTGRES_PASSWORD: specmap_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
